# 02 · Color

- `paleta.md`             — sistema de color completo (neutros + escaleras 50–900 + acentos por producto) con HEX/RGB y tokens.
- `paleta-completa.png`   — poster de la paleta completa (neutros, escaleras y acentos).
- `mockups-logo.png`      — logo en distintos colores sobre distintos fondos de marca.
- `swatches/`             — muestras PNG 400×400 de cada color (incluye `hueso` y `carbon`).
- `scales/`               — tiras PNG de cada escalera tonal (`escalera-neutro`, `-bosque`, `-slate`, `-salvia`, `-terracota`).
